nav(class="navbar navbar-expand-lg navbar-dark bg-light").shadow
    .container-fluid
        img(src='https://www.freepnglogos.com/uploads/apple-music-logo-circle-png-28.png' ,style='width:2rem')
        a(class="navbar-brand" href="#" style="margin-left: 1rem").text-dark MyMusic
        form.d-flex(action='/user/home' , method="GET" style=' width: 30%; margin-left : 25%; margin-top:1rem ;')
            input.form-control.me-2(type='search' placeholder='Search' aria-label='Search', name='q')
            button.btn.btn-outline-primary(type='submit') Search
        div(class="collapse navbar-collapse" id="navbarNavAltMarkup")
        div(class="navbar-nav").text-dark
            //- a(class="nav-link" href="/contact-us").text-dark Contact Us
            if user
                a(class="nav-link active" aria-current="page" href="/user/home").text-dark Home
                a(class="nav-link" href=href='/user/edit/'+user._id).text-dark #{user.name}
                a(class="nav-link" href="/user/logout").text-dark Logout
            else
                a(class="nav-link" href="/sign-up").text-dark Sign-up
                a(class="nav-link" href="/login").text-dark Login



